{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(jq -r '.tool_input.path // .tool_input.filePath // empty'); if [ -n \"$file_path\" ] && [ -f \"$file_path\" ]; then ruff format \"$file_path\" || echo \"ruff format failed for $file_path\"; fi"
          }
        ]
      }
    ]
  }
}
